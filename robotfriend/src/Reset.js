import React from 'react';

const Reset = () => {
    return (
        <div>
            <button 
            className=' pa3 ba w-m b--green bg-lightest-blue'
            // onClick={reload}>
            >Reset the name</button>
        </div>
    );
}

export default Reset;